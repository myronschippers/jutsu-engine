const express = require('express');
const pool = require('../modules/pool');
const router = express.Router();

/**
 * GET all of my custom Ninjas.
 */
router.get('/', (req, res) => {
    
});

/**
 * POST a new custom Ninja to my ninjas.
 */
router.post('/', (req, res) => {

});

module.exports = router;